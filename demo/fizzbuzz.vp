bool func isDivisible(int x, int div) => (x % div == 0);

bool divThree;
bool divFive;

for(int i = 0; i <= 100; i++) {
    divThree = isDivisible(i, 3);
    divFive = isDivisible(i, 5);
    
    if(divThree) {
        print("fizz");
    } else if(divFive) {
        print("buzz");
    }
}
