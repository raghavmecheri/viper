int func factorial(int target) {
    if(target == 1) {
        print(target);
        return target;
    }
    
    int sub = factorial(target-1);
    int res = target * sub;
    print(res);
    return res;
}

print("Factorial of 10:");
factorial(10);
