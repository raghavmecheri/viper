int func calc(int a, int b, char op) {
    int res = ??
        op == "+" : a + b
      | op == '-' : a - b
      | op == '*' : a * b
      | op == '/' : a / b
    ?? a

    return res
}

int result = calc(10, 20, '+')
print(result);
